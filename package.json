{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.15.2",
    "socket.io": "^1.7.4"
  },
  "scripts": {
    "start": "flow-remove-types ./ -d ./lib/ --all --pretty; node lib/index.js",
    "flow": "flow",
    "flow:check": "flow check ./",
    "flow:build": "flow-remove-types ./ -d ./lib/ --all --pretty"
  },
  "devDependencies": {
    "flow-bin": "^0.109.0",
    "flow-remove-types": "^2.109.0"
  }
}
