{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "bcrypt": "^3.0.7",
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.4",
    "cookie-session": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.15.2",
    "express-fileupload": "^1.1.6",
    "express-session": "^1.17.0",
    "flow-bin": "^0.109.0",
    "flow-remove-types": "^2.110.1",
    "helmet": "^3.21.2",
    "ibm-cos-sdk": "^1.6.0",
    "redis": "^2.8.0",
    "request": "^2.88.0",
    "request-promise": "^4.2.5",
    "skipper": "^0.8.7",
    "socket.io": "^1.7.4",
    "socket.io-redis": "^4.0.1"
  },
  "scripts": {
    "start": "rm -R -f lib && flow-remove-types ./ -d ./lib/ -p --all -x .html,.css,.js --quiet; node lib/index.js",
    "start-local": "rm -R -f lib && flow-remove-types ./ -d ./lib/ -p --all -x .html,.css,.js --quiet; node lib/index.js local",
    "flow": "flow",
    "flow:check": "flow check ./",
    "flow:build": "flow-remove-types ./ -d ./lib/ --all --pretty"
  }
}
